pipeline {
  agent any
  if (params.BRANCH_NAME == "master"){
    stages {
      stage('Prepare') {
        steps {
          echo "--------------------Prepare Stage---------------------"
        }
      }
    }
  }
}
