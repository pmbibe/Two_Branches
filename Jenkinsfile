pipeline {
  agent any
  if (params.BRANCH == "master"){
    stages {
      stage('Prepare') {
        steps {
          echo "--------------------Prepare Stage---------------------"
        }
      }
    }
  }
}
